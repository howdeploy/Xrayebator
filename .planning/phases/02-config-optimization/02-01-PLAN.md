---
phase: 02-config-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xrayebator
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-05
  - CFG-06
  - CFG-07

must_haves:
  truths:
    - "Existing installations receive all 6 optimizations on first menu launch after update"
    - "DNS queries use DoH Local (https+local://) unless AdGuard Home is active"
    - "Freedom outbound resolves domains to IPv4 only (UseIPv4)"
    - "All inbound sniffing uses routeOnly:true (no destination override)"
    - "Access log is disabled for privacy"
    - "Policy bufferSize=4 reduces memory usage per connection"
    - "BitTorrent traffic is blocked via routing rule"
    - "Migration is skipped entirely on subsequent launches (marker file)"
    - "New inbounds created after migration also have routeOnly:true"
    - "AdGuard Home uninstall restores DNS to https+local:// format (not old AdGuard DoH)"
  artifacts:
    - path: "xrayebator"
      provides: "migrate_config_optimization() function and updated inbound templates"
      contains: "migrate_config_optimization"
  key_links:
    - from: "main_menu() migration runner"
      to: "migrate_config_optimization()"
      via: "marker file .config_optimized check"
      pattern: "\\.config_optimized"
    - from: "migrate_config_optimization()"
      to: "safe_jq_write"
      via: "all 6 sub-migrations use safe_jq_write"
      pattern: "safe_jq_write"
    - from: "migrate_config_optimization()"
      to: "backup_config"
      via: "backup before any mutations"
      pattern: "backup_config.*config_optimization"
---

<objective>
Add migrate_config_optimization() function to xrayebator with all 6 config optimizations (DNS DoH Local, Freedom UseIPv4, sniffing routeOnly, access log none, policy bufferSize, BitTorrent block), wire it into main_menu() migration runner, update all 4 inbound templates to include routeOnly:true, and fix AdGuard Home uninstall DNS restore format.

Purpose: Existing installations automatically receive optimal config on first launch after update. New inbounds also get routeOnly from the start. AdGuard Home uninstall no longer restores to deprecated DNS format.
Output: Modified xrayebator with migration function, updated templates, and fixed uninstall DNS restore.
</objective>

<execution_context>
@/home/kosya/.claude/get-shit-done/workflows/execute-plan.md
@/home/kosya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-optimization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migrate_config_optimization() function and wire into main_menu()</name>
  <files>xrayebator</files>
  <action>
1. Add the `migrate_config_optimization()` function BEFORE the `# END HELPER FUNCTIONS` comment (insert near line 449, before `SERVER_IP=$(get_server_ip)`). The function must:

   a. Print header: `echo -e "${CYAN}Оптимизация конфигурации...${NC}"`
   b. Call `backup_config "config_optimization"` before any mutations
   c. Track `local changed=false`

   d. **CFG-01 DNS DoH Local**: Check `jq -r '.dns.servers[0] // ""' "$CONFIG_FILE"`. If `"127.0.0.1"` -- print preserved (AdGuard Home), skip. If already `"https+local://1.1.1.1/dns-query"` -- print already optimized, skip. Otherwise, use `safe_jq_write` to set `.dns` to:
      ```json
      {"servers":["https+local://1.1.1.1/dns-query","localhost"],"queryStrategy":"UseIPv4","disableCache":false}
      ```
      Set `changed=true` on success.

   e. **CFG-02 Freedom UseIPv4**: Check `jq -r '.outbounds[] | select(.protocol == "freedom") | .settings.domainStrategy // "none"'`. If not `"UseIPv4"`, use `safe_jq_write` with path assignment (NOT object merge -- preserves existing settings like fragment):
      ```
      (.outbounds[] | select(.protocol == "freedom")).settings.domainStrategy = "UseIPv4"
      ```
      IMPORTANT: Use specific path assignment to not clobber existing .settings fields (e.g. fragment anti-DPI settings).

   f. **CFG-03 Sniffing routeOnly**: Check `jq '[.inbounds[] | select(.sniffing.routeOnly != true)] | length'`. If > 0, use `safe_jq_write`:
      ```
      .inbounds[] |= (if .sniffing then .sniffing.routeOnly = true else . end)
      ```
      This is defensive -- only modifies inbounds that have a sniffing object.

   g. **CFG-05 Access log none**: Check `jq -r '.log.access // "not_set"'`. If not `"none"`, use `safe_jq_write '.log.access = "none"'`.

   h. **CFG-06 Policy bufferSize**: Check `jq '.policy // empty'`. If empty/null, use `safe_jq_write` to add:
      ```json
      {"levels":{"0":{"handshake":4,"connIdle":300,"uplinkOnly":2,"downlinkOnly":5,"bufferSize":4}}}
      ```

   i. **CFG-07 BitTorrent block**: Check `jq -r '[.routing.rules[] | select(.protocol != null) | .protocol[] | select(. == "bittorrent")] | length'`. If `"0"` or empty, insert BEFORE the last rule (catch-all direct):
      ```
      .routing.rules |= (.[:-1] + [{"type":"field","protocol":["bittorrent"],"outboundTag":"block"}] + .[-1:])
      ```

   j. Call `fix_xray_permissions` after all mutations.
   k. Return 0 if `changed=true` (needs restart), return 1 if no changes.

   Each sub-step prints status with color: YELLOW for "migrating", GREEN for "already done" or success, RED for error. All user-facing strings in Russian (match existing pattern).

2. Wire migration into `main_menu()` migration runner block (after the XHTTP extra restore migration, around line 516). Add:
   ```bash
   # Config optimization migration (Phase 2)
   if [[ ! -f /usr/local/etc/xray/.config_optimized ]]; then
     if migrate_config_optimization; then
       touch /usr/local/etc/xray/.config_optimized
       needs_restart=true
     fi
   fi
   ```
   This must be BEFORE the `if [[ "$needs_restart" == "true" ]]` restart block.

All user-facing strings must be in Russian. Follow existing migration function style exactly (see migrate_routing_config, migrate_xhttp_mode patterns).
  </action>
  <verify>
Run `bash -n xrayebator` to verify syntax. Then grep to confirm:
- `grep -c "migrate_config_optimization" xrayebator` returns 2+ (function definition + call in main_menu)
- `grep -c "config_optimized" xrayebator` returns 2 (marker file check + touch)
- `grep -c "safe_jq_write" xrayebator` inside migrate_config_optimization shows 6 potential calls (one per sub-migration)
- `grep "backup_config.*config_optimization" xrayebator` returns 1
- `grep "fix_xray_permissions" xrayebator` appears inside the function
  </verify>
  <done>
migrate_config_optimization() exists with all 6 sub-migrations (CFG-01 through CFG-07, skipping CFG-04 which is Phase 1). Each sub-migration is idempotent, uses safe_jq_write, prints Russian status messages. Function is called from main_menu() with .config_optimized marker file. AdGuard Home DNS (127.0.0.1) is preserved. Freedom settings use path assignment (not object merge). BitTorrent rule is inserted before catch-all direct rule.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update inbound templates and AdGuard Home uninstall DNS restore</name>
  <files>xrayebator</files>
  <action>
1. **Update all 4 inbound templates in add_inbound()** (lines 864, 893, 923, 967 area) to add `"routeOnly": true` to the sniffing JSON object. Change every occurrence of:
   ```json
   "sniffing": {"enabled": true, "destOverride": ["http", "tls", "quic"]}
   ```
   to:
   ```json
   "sniffing": {"enabled": true, "destOverride": ["http", "tls", "quic"], "routeOnly": true}
   ```
   There are exactly 4 occurrences (tcp-mux, tcp/tcp-utls/tcp-xudp, grpc, xhttp templates).

2. **Fix AdGuard Home uninstall DNS restore** in `uninstall_adguard_home()` (around line 2340). The current code restores DNS to the old AdGuard DoH format with complex server objects. Change to use the new `https+local://` format:
   - Replace the `.dns = {...}` block in the safe_jq_write call to:
     ```json
     {
       "servers": [
         "https+local://1.1.1.1/dns-query",
         "localhost"
       ],
       "queryStrategy": "UseIPv4",
       "disableCache": false
     }
     ```
   - Update the confirmation message from `"Xray DNS -> AdGuard DoH"` to `"Xray DNS -> DoH Local"`
   - Update the UI text before the confirmation prompt (line 2312 area) from "AdGuard DoH (dns.adguard-dns.com)" to "DoH Local (1.1.1.1)" so the user knows what DNS will be restored to.
  </action>
  <verify>
Run `bash -n xrayebator` to verify syntax. Then:
- `grep -c 'routeOnly.*true' xrayebator` returns 5+ (4 templates + at least 1 in migration function)
- `grep -c '"sniffing".*routeOnly' xrayebator` returns 4 (one per inbound template)
- Verify NO remaining `"sniffing": {"enabled": true, "destOverride":` without `routeOnly` in the 4 template blocks
- `grep 'https+local.*1.1.1.1' xrayebator` appears in uninstall_adguard_home function
- `grep -c 'dns.adguard-dns.com' xrayebator` returns 0 (no references to old format remain)
  </verify>
  <done>
All 4 inbound templates include routeOnly:true in sniffing (new profiles get it from the start). AdGuard Home uninstall restores DNS to https+local://1.1.1.1/dns-query format instead of deprecated AdGuard DoH format. No references to dns.adguard-dns.com remain in xrayebator.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `bash -n xrayebator` passes (syntax valid)
2. Migration function exists with 6 sub-migrations using safe_jq_write
3. Migration is wired into main_menu() with marker file
4. All 4 inbound templates have routeOnly:true
5. AdGuard uninstall restores to https+local:// format
6. No raw jq temp file patterns in new code (all safe_jq_write)
7. No references to dns.adguard-dns.com in xrayebator
</verification>

<success_criteria>
- migrate_config_optimization() implements CFG-01, CFG-02, CFG-03, CFG-05, CFG-06, CFG-07
- Each sub-migration is idempotent (safe to run on already-migrated configs)
- AdGuard Home DNS (127.0.0.1) is never overwritten
- Freedom outbound settings use path assignment (preserves fragment settings)
- BitTorrent rule is inserted before catch-all direct rule
- All 4 inbound templates include routeOnly:true for new profiles
- AdGuard Home uninstall restores to new DNS format
- bash -n xrayebator passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-optimization/02-01-SUMMARY.md`
</output>
