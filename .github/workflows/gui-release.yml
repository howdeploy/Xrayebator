name: GUI desktop release

on:
  pull_request:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Optional gui-v* tag to publish as a prerelease"
        required: false
        type: string
  push:
    tags:
      - "gui-v*"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            gui-legacy/pyproject.toml
            gui-legacy/packaging/constraints.txt
            gui-legacy/packaging/requirements-build.txt
      - name: Install system GUI libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libegl1 libgl1 libxkbcommon0 libdbus-1-3 libfontconfig1 libxcb-cursor0
      - name: Install test dependencies
        run: >-
          python -m pip install
          --constraint gui-legacy/packaging/constraints.txt
          --requirement gui-legacy/packaging/requirements-build.txt
          --editable "./gui-legacy[dev]"
      - name: Check package
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          python -m pip check
          python -m ruff check --config gui-legacy/pyproject.toml gui-legacy
          python -m pytest -q gui-legacy/tests
          python -m build --wheel gui-legacy

      # Bash smoke subset kept here for fast feedback on GUI-PR touching bash;
      # the FULL suite (all validation/test-*.sh) runs in ci-linux.yml.
      - name: Bash syntax smoke (GUI job)
        run: |
          for test in \
            validation/test-quickstart-migration-parity.sh \
            validation/test-quickstart-subscription-port.sh \
            validation/test-vless-url-generation.sh; do
            echo "=== $test ==="
            # CRLF safe: tests already normalize via tr -d '\r' internally
            bash "$test"
          done
          # Syntax check of core bash files (POSIX grep on Linux; Git Bash on Windows)
          bash -n xrayebator
          bash -n install.sh
          bash -n update.sh
          bash -n uninstall.sh

  build:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-2025
            artifact_name: Xrayebator-Windows-x64
            artifact_file: Xrayebator-Windows-x64.exe
            platform: windows
          - runner: macos-15-intel
            artifact_name: Xrayebator-macOS-Intel
            artifact_file: Xrayebator-macOS-Intel.dmg
            platform: macos
          - runner: macos-15
            artifact_name: Xrayebator-macOS-Apple-Silicon
            artifact_file: Xrayebator-macOS-Apple-Silicon.dmg
            platform: macos
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            gui-legacy/pyproject.toml
            gui-legacy/packaging/constraints.txt
            gui-legacy/packaging/requirements-build.txt
      - name: Install build dependencies
        run: >-
          python -m pip install
          --constraint gui-legacy/packaging/constraints.txt
          --requirement gui-legacy/packaging/requirements-build.txt
          ./gui-legacy
      - name: Bundle verified Xray core
        run: python gui-legacy/packaging/fetch_xray.py
      - name: Build native application
        run: python gui-legacy/packaging/build.py
      - name: Smoke-test Windows executable
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          $env:QT_QPA_PLATFORM = "offscreen"
          & "gui-legacy/dist/Xrayebator.exe" --self-test
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Package Windows executable
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force gui-legacy/artifacts | Out-Null
          Copy-Item gui-legacy/dist/Xrayebator.exe "gui-legacy/artifacts/${{ matrix.artifact_file }}"
          python gui-legacy/packaging/checksum.py "gui-legacy/artifacts/${{ matrix.artifact_file }}"
      - name: Smoke-test macOS application
        if: matrix.platform == 'macos'
        run: |
          QT_QPA_PLATFORM=offscreen \
            gui-legacy/dist/Xrayebator.app/Contents/MacOS/Xrayebator --self-test
      - name: Package macOS disk image
        if: matrix.platform == 'macos'
        run: |
          mkdir -p gui-legacy/artifacts/dmg-root
          cp -R gui-legacy/dist/Xrayebator.app gui-legacy/artifacts/dmg-root/
          ln -s /Applications gui-legacy/artifacts/dmg-root/Applications
          hdiutil create \
            -volname Xrayebator \
            -srcfolder gui-legacy/artifacts/dmg-root \
            -ov \
            -format UDZO \
            "gui-legacy/artifacts/${{ matrix.artifact_file }}"
          python gui-legacy/packaging/checksum.py "gui-legacy/artifacts/${{ matrix.artifact_file }}"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            gui-legacy/artifacts/${{ matrix.artifact_file }}
            gui-legacy/artifacts/${{ matrix.artifact_file }}.sha256
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/gui-v') || inputs.release_tag != ''
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - name: Publish GitHub prerelease
        env:
          GH_TOKEN: ${{ github.token }}
          REQUESTED_TAG: ${{ inputs.release_tag }}
        run: |
          tag="${GITHUB_REF_NAME}"
          if [[ "${GITHUB_REF_TYPE}" != "tag" ]]; then
            tag="${REQUESTED_TAG}"
          fi
          [[ "${tag}" == gui-v* ]] || {
            echo "Release tag must start with gui-v" >&2
            exit 2
          }
          gh release create "${tag}" artifacts/* \
            --repo "${GITHUB_REPOSITORY}" \
            --target "${GITHUB_SHA}" \
            --title "Xrayebator GUI ${tag#gui-v}" \
            --generate-notes \
            --prerelease
